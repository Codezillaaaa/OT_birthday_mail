# OpenTalk Birthday Mail Service - Dependencies
# Updated for Render free tier deployment

# Web framework
Flask==3.0.0
Werkzeug==3.0.1

# WSGI server for production
gunicorn==21.2.0

# MongoDB driver with SRV support
pymongo==4.6.1
dnspython==2.4.2

# Timezone support (fallback for Python < 3.9)
pytz==2024.1

# Email service (HTTP API - works on Render free tier)
resend==2.0.0
